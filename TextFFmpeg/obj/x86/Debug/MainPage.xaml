<Page
    x:Class="TextFFmpeg.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TextFFmpeg"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <SplitView x:ConnectionId='2' x:Name="Splitter">

            <!-- Control panel -->
            <SplitView.Pane>
                <StackPanel VerticalAlignment="Stretch" Margin="10, 50, 0, 0">
                    <TextBlock x:ConnectionId='4' x:Name="Header" Text="Open Local File or URI"/>
                    <StackPanel Orientation="Horizontal">
                        <AppBarButton x:ConnectionId='7' Content="Open Local File" Icon="OpenFile" Margin="0, 10, 10, 0"                      />
                        <TextBox x:ConnectionId='8' PlaceholderText="Enter URI and press Enter" InputScope="Url" Width="210" Height="30"                    />
                    </StackPanel>
                    <ToggleSwitch x:ConnectionId='5' x:Name="toggleSwitchAudioDecode" Header="Force Decode Audio" OffContent="OFF" OnContent="ON" IsOn="False"/>
                    <ToggleSwitch x:ConnectionId='6' x:Name="toggleSwitchVideoDecode" Header="Force Decode Video" OffContent="OFF" OnContent="ON" IsOn="False"/>
                </StackPanel>
            </SplitView.Pane>

            <!-- Main content -->
            <MediaElement x:ConnectionId='9' x:Name="mediaElement"
                        AreTransportControlsEnabled="True"
                        AudioCategory="ForegroundOnlyMedia"
                        AutoPlay="True"
                                                 
            />

        </SplitView>

        <!-- Control panel toggle button -->
        <ToggleButton VerticalAlignment="Top" Margin="10, 10, 0, 0" IsChecked="{Binding ElementName=Splitter, Path=IsPaneOpen, Mode=TwoWay}">
            <ToggleButton.Content>
                <FontIcon x:ConnectionId='3' x:Name="Hamburger" FontFamily="Segoe MDL2 Assets" Foreground="White" Glyph="&#xE700;"/>
            </ToggleButton.Content>
        </ToggleButton>
    </Grid>
</Page>

